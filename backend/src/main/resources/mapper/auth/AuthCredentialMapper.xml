<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bible.module.auth.mapper.AuthCredentialMapper">

    <resultMap id="AuthCredentialResultMap" type="com.bible.module.auth.entity.AuthCredential">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="password_updated_at" property="passwordUpdatedAt"/>
        <result column="failed_login_count" property="failedLoginCount"/>
        <result column="locked_until" property="lockedUntil"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findByUserId" resultMap="AuthCredentialResultMap">
        SELECT * FROM auth_credentials WHERE user_id = #{userId}
    </select>

    <insert id="insert">
        INSERT INTO auth_credentials(id, user_id, password_hash, password_updated_at, failed_login_count, locked_until, created_at, updated_at)
        VALUES(gen_random_uuid(), #{userId}, #{passwordHash}, #{passwordUpdatedAt}, #{failedLoginCount}, #{lockedUntil}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE auth_credentials
        SET failed_login_count = #{failedLoginCount}, locked_until = #{lockedUntil}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE auth_credentials
        SET password_hash = #{passwordHash}, password_updated_at = #{now}, updated_at = NOW()
        WHERE user_id = #{userId}
    </update>

</mapper>